Place the supplied print header image in this folder with the exact filename:

BUCOR HEADER.png

The V2 profile preview does not show this image on screen. It is displayed only in the browser Print layout.
